[project]
name = "am_israel_hai_badge"
version = "0.1.0"
requires-python = ">= 3.11.9"
dependencies = []

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends._legacy:_Backend"

[tool.setuptools.packages.find]
where = ["src"]
